export interface TemplateStyleConfig {
  primaryColor: string;
  secondaryColor: string;
  accentColor: string;
  fontFamily?: string;
  headerStyle?: 'centered' | 'default' | 'gradient';
  sectionStyle?: 'bordered' | 'card' | 'minimal' | 'default';
  name: string;
  description: string;
  color: string;
}
